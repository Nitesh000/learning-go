package main

import "fmt"

// all the import modules will be present here.
// if there is a module which is not used but imported then it will show warning in some cases.
// the lexer is desined that way.
// You can use some kind of auto formatter so it will just remove the module/modules.
// That way it will be great.

func main() {
	fmt.Println("Hello, World!")
}
